#include "letters.h"

static struct string words[] = {
	/* This line is replaced at build time by the words in DICTIONARY */
};

struct dictionary default_dict = {
	.index = words,
	.cap = 0,
	.len = sizeof words / sizeof *words
};
